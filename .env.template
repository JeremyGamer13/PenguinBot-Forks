# CORE TOKENS
TOKEN="discord bot token..."
TOKEN_TEST="discord bot token..."
TOKEN_PERSONAL="discord bot token..."

# SETTINGS
PREFIX="jg!"
PREFIX_TEST="jg!"
PREFIX_PERSONAL="jg!"
PREFIX_PRESETS="."
PREFIX_PRESETS_TEST="."
PREFIX_PRESETS_PERSONAL="."
OWNER="462098932571308033"
SERVER_ID="1033551490331197462"
CHECK_FOR_DEFAULT_TEST_SERVERS="true" # some code may only run in the defined server id or (if enabled) default PenguinBot Testing servers
RESPOND_TO_KEYWORDS="false"
PREVENT_UPDATES="false"
DISABLE_GIT="true" # disables the use of Git CLI (for commands like pm!pull)
DISABLE_GIT_WRITE="true" # disables the use of Git CLI for writing to a repo (for commands like pm!push)
DISABLE_FORKING="false" # Disables the use of multi-threading in the multi-thread module, specifically child_process.fork()
DEVICE_LABEL="computer" # jg: Just a visual label to tell what device the bot is running on
STATE_DEFAULT_PANELFORCEDDISABLED="true" # sets panelForcedDisabled on startup
STATE_DEFAULT_NODEAPIOVERLAYS="false" # jg: is this enabled by default?
STATE_DEFAULT_NODEAPIOVERLAYS_PERSONAL="true" # jg: makes nodeApiOverlays enabled by default if running in personal mode (overrides STATE_DEFAULT_NODEAPIOVERLAYS)
STATE_DEFAULT_NODEAPIAUDIOS="false" # jg: is this enabled by default?
STATE_DEFAULT_NODEAPIAUDIOS_PERSONAL="true" # jg: makes nodeApiAudios enabled by default if running in personal mode (overrides STATE_DEFAULT_NODEAPIAUDIOS)
STATE_DEFAULT_NODEAPIPRESETCOOLDOWN="15000" # jg: Default nodeApiPresetCooldown in milliseconds
STATE_DEFAULT_HEAVYEXTERNALSTUFF="false" # jg: is this enabled by default?

# WEBHOOKS
SANTA_LIST_WEBHOOK="discord webhook url..."
SANTA_WISHLIST_WEBHOOK = "discord webhook url..."
CHANNEL_REWRITE_WEBHOOK="discord webhook url..."

# NODE API
JGNODEAPI_ENABLED="false"
JGNODEAPI_URL="http://localhost:6767"
JGNODEAPI_TOKEN="node api token..." # Some jg_node_api instances may enforce token authorization for security
JGNODEAPI_SWITCHES_LISTEN="false" # listen for jg_node_api switches
JGNODEAPI_SWITCHES_LISTEN_JLAPTOP="false" # listen for jlaptop switches
JGNODEAPI_PRESETS_PREVIEW_FOLDER_AUDIO="C:/Users/Jeremy/Documents/GitHub/jg_node_api/assets/audio" # Certain presets can be previewed in the help command. Configure a folder to grab the assets from.
JGNODEAPI_PRESETS_PREVIEW_FOLDER_VIDEO="C:/Users/Jeremy/Documents/GitHub/jg_node_api/assets/video" # Certain presets can be previewed in the help command. Configure a folder to grab the assets from.

# VLC
VLC_MEDIA_ENABLED="false"
VLC_MEDIA_PORT="8080" # not the telnet port, you can configure web int port with vlc.exe --extraintf http --http-port 9090
VLC_MEDIA_PASSWORD="web interface password (not telnet)"
VLC_MEDIA_ALLOWED_FOLDER="file:///C:/Users/Jeremy/Music/"

# OLLAMA
OLLAMA_ENABLED="false"
OLLAMA_URL="http://localhost:11434"

# OLLAMA + ROB INTEGRATION THROUGH DISCORD (https://dogo6647.github.io/rob/) (ollama talks to rob back and forth)
ROB_INTEGRATION_ENABLED="false" # Whether or not rob integration is enabled in public mode.
ROB_INTEGRATION_ENABLED_PERSONAL="false" # Whether or not rob integration is enabled in personal mode.
ROB_INTEGRATION_USER_ID="1344543448719429673" # The user ID of Rob. Likely will never change.

# SEARXNG
SEARXNG_ENABLED="false" # Whether or not SearXNG is available.
SEARXNG_URL="http://127.0.0.1:8080" # The URL of the SearXNG instance
SEARXNG_PRIVATE_ENGINES_TOKEN="" # A tokens cookie to add to requests for the sake of using private engines

# STAMMER
STAMMER_ENABLED="false"
STAMMER_PATH="C:/Users/Jeremy/Downloads/TechnicalStuff/stammer" # the folder with stammer.py in it
STAMMER_PYTHON="python stammer.py {{CARRIER}} {{MODULATOR}} {{OUTPUT}}"

# DEMUCS
DEMUCS_ENABLED="false"
DEMUCS_EXECUTABLE="demucs"

# RVC & AI COVERS
RVC_ENABLED="false"
RVC_PATH="C:/Users/Jeremy/Documents/GitHub/jeremy-stream-bot/src/resources/python/rvc" # usually just set this to the absolute path of src/resources/python/rvc as it contains rvc.py inside
RVC_PYTHON="python rvc.py --method {{METHOD}} --semitones {{SEMITONES}} --model {{MODEL}} --index {{INDEX}} --input {{INPUT}} --output {{OUTPUT}}"

# CHATTERBOX & DIRECT AI VOICE TTS
CHATTERBOX_ENABLED="false"
CHATTERBOX_PATH="C:/Users/Jeremy/Documents/GitHub/jeremy-stream-bot/src/resources/python/chatterbox" # usually just set this to the absolute path of src/resources/python/chatterbox
CHATTERBOX_PATH_MODEL="E:/AIModels/Chatterbox-torch-2.6.0+cu118-cp310/HF_HOME/hub/models--ResembleAI--chatterbox/snapshots/5bb1f6ee58e50c3b8d408bc82a6d3740c2db6e18" # Download the ResembleAI/chatterbox ChatterboxTTS model here (you should have like 5 files and one of them is tokenizer.json)
CHATTERBOX_MAX_LENGTH="2048" # The max length string that the chatterbox script is allowed to chunk and speak in full
CHATTERBOX_MAX_CHUNKS="64" # The max amount of chunks that the chatterbox script is allowed to speak (every tag makes a chunk & around every 80 chars makes a new chunk)
CHATTERBOX_PYTHON_EXEC="python"
CHATTERBOX_PYTHON_CODE="cuda.py" # see chatterbox in util folder for implementation

# OBJDETECT (object detection in image, that key name isnt the name of any specific model or anyting)
OBJDETECT_ENABLED="false"
OBJDETECT_PATH="C:/Users/Jeremy/Documents/GitHub/jeremy-stream-bot/src/resources/python/objectdetection" # usually just set this to the absolute path of src/resources/python/objectdetection
OBJDETECT_PATH_MODEL="E:/AIModels/grounding-dino-base-torch-2.7.1+cu118-cp310/HF_HOME/hub/models--IDEA-Research--grounding-dino-base/snapshots/12bdfa3120f3e7ec7b434d90674b3396eccf88eb" # Download the IDEA-Research/grounding-dino-base AutoProcessor and AutoModelForZeroShotObjectDetection models here (you should have like 6 or 7 files and one of them is vocab.txt)
OBJDETECT_PYTHON_EXEC="python"
OBJDETECT_PYTHON_CODE="groundingdino_detector.py" # see object-detection in util folder for implementation

# REMOVEBACKGROUND (background removal, that key name isnt the name of any specific model or anyting)
REMOVEBACKGROUND_ENABLED="false"
REMOVEBACKGROUND_PATH="C:/Users/Jeremy/Documents/GitHub/jeremy-stream-bot/src/resources/python/removebackground" # usually just set this to the absolute path of src/resources/python/removebackground
REMOVEBACKGROUND_PYTHON="python removebackground.py --input {{INPUT}} --output {{OUTPUT}}"

# PSOLA for vocoding (cheap it just requires external python setup so its annoying)
PSOLA_ENABLED="false"
PSOLA_PATH="C:/Users/Jeremy/Documents/GitHub/jeremy-stream-bot/src/resources/python/psola" # usually just set this to the absolute path of src/resources/python/psola as it contains script.py inside
PSOLA_PYTHON="python script.py --task {{TASK}} --frequency {{FREQUENCY}} --input {{INPUT}} --output {{OUTPUT}}"

# FLUIDSYNTH for midi rendering (cheap but uses external app)
FLUIDSYNTH_ENABLED="false"
FLUIDSYNTH_EXEC="fluidsynth"
FLUIDSYNTH_SOUNDFONT="C:/Users/Jeremy/Documents/GitHub/jeremy-stream-bot/assets/midi/gm.dls" # .sf2, .sf3, .dls

# MinecraftWatchdog
MINECRAFT_WATCHDOG_ENABLED="false"
MINECRAFT_WATCHDOG_PORT="6767"

# sekai-stickers https://github.com/TheOriginalAyaka/sekai-stickers
SEKAI_STICKERS_PATH="" # set to blank if not cloned. use the root folder path here

# OTHER CRAP